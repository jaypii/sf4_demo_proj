{% extends 'base.html.twig' %}

{% block title %}Blog{% endblock %}

{% block body %}
<h1>Blog List</h1>
<div class="card">
    <div class="card-body">
        <p><a href="{{ path('blog_show', {'slug': 'test'}) }}">Show</a></p>
    </div>
</div>
{% endblock %}