{% extends 'base.html.twig' %}

{% block title %}New TestEnt{% endblock %}

{% block body %}
    <h1>Create new entry</h1>
    <div class="row">
        <div class="col">
            {{ include('test_ent/_form.html.twig') }}
        </div>
        <div class="col">
            &nbsp;
        </div>
    </div>
    <a href="{{ path('test_ent_index') }}" class="btn btn-primary" role="button">Back to list</a>
{% endblock %}
